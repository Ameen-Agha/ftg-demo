apiVersion: v1
kind: Service
metadata:
  name: {{ include "ftg-chart.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "ftg-chart.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080